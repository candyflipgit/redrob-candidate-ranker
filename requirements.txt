# Ranking-step dependencies ONLY (what scripts/rank.py needs).
# Deliberately minimal + CPU-only + no network — this is the set reproduced at
# Stage 3 inside the sandboxed container. No torch / transformers here: the
# ranking step loads precomputed embeddings as plain .npy and uses numpy.
numpy==2.4.4
pandas==2.3.3
scipy==1.17.1
scikit-learn==1.8.0
PyYAML>=6.0
jsonschema>=4.20
